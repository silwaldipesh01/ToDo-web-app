using Microsoft.AspNetCore.SignalR;

namespace ToDo_App.Hubs
{
    public class TodoHub : Hub
    {
        //public async Task UpdatedTodo(int todoId)
        //{
        //    await Groups.AddToGroupAsync(Context.ConnectionId, todoId.ToString());
        //}
    }
}
